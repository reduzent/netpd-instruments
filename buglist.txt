qseq3
  
  * sub- / instr selectors are not init'ed on instr list change
  
  * subinstr list selector doesn't update on instr list selection


master

  * when two clients start simultaneously, one of the two new
    master instances (or both?) overwrites current settings
